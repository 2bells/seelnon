// This file is intended for future Pixel Editor settings.
// For now, it can remain empty or contain comments.

export const defaultPixelEditorSettings = {
  gridColor: 'rgba(0, 0, 0, 0.3)',
  defaultBrushSize: 1,
  onionSkin: {
    enabled: false,
    mode: 'previous', // 'previous' or 'next'
    opacity: 0.5,
  }
};